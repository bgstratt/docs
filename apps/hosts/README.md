# Host apps workspace

This workspace holds local host programs used by demos/playgrounds.

Primary goal:

- Run demo host behavior from package-consumption mode inside this repo.

Current target:

- `nodalmerge-demo-host/` using NodalMerge NuGet packages, including native runtime assets.

